<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_invoice_fel_document"
              name="Portal invoice FEL document"
              inherit_id="account.portal_invoice_page"
              priority="40">
        <xpath expr="//div[hasclass('o_download_pdf')]" position="inside">
            <t t-set="fel_document_url"
               t-value="invoice._get_portal_fel_document_url()"/>
            <a t-if="fel_document_url"
               class="btn btn-primary flex-grow-1"
               t-att-href="fel_document_url"
               target="_blank"
               rel="noopener noreferrer"
               title="Abrir documento certificado FEL"
               role="button">
                <i class="fa fa-external-link"/>
                Ver documento FEL
            </a>
            <button t-else=""
                    type="button"
                    class="btn btn-outline-secondary flex-grow-1"
                    data-bs-toggle="modal"
                    data-bs-target="#felNotCertifiedModal">
                <i class="fa fa-file-text-o"/>
                Documento FEL
            </button>
        </xpath>
        <xpath expr="//div[hasclass('o_portal_invoice_sidebar')]" position="after">
            <div t-if="not invoice._get_portal_fel_document_url()"
                 class="modal fade"
                 id="felNotCertifiedModal"
                 tabindex="-1"
                 aria-labelledby="felNotCertifiedModalLabel"
                 aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="felNotCertifiedModalLabel">
                                Documento FEL no disponible
                            </h5>
                            <button type="button"
                                    class="btn-close"
                                    data-bs-dismiss="modal"
                                    aria-label="Cerrar"/>
                        </div>
                        <div class="modal-body">
                            <p t-if="invoice.fel_documento_certificado" class="mb-0">
                                La factura tiene un enlace FEL registrado, pero no es una
                                URL HTTPS válida de Feel. Solicita una revisión interna.
                            </p>
                            <p t-else="" class="mb-0">
                                La factura <strong t-out="invoice.name"/> todavía no ha
                                sido certificada y no existe un documento FEL para descargar.
                            </p>
                        </div>
                        <div class="modal-footer">
                            <button type="button"
                                    class="btn btn-secondary"
                                    data-bs-dismiss="modal">
                                Cerrar
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_invoice_list_fel_document"
              name="Portal invoice list FEL document"
              inherit_id="account.portal_my_invoices"
              priority="40">
        <xpath expr="//th[@name='status']" position="after">
            <th name="fel_document" class="d-none d-lg-table-cell">
                Documento FEL
            </th>
        </xpath>
        <xpath expr="//td[@name='invoice_status']" position="after">
            <td name="invoice_fel_document" class="d-none d-lg-table-cell">
                <t t-set="fel_document_url"
                   t-value="invoice._get_portal_fel_document_url()"/>
                <a t-if="fel_document_url"
                   class="btn btn-sm btn-outline-primary"
                   t-att-href="fel_document_url"
                   target="_blank"
                   rel="noopener noreferrer">
                    <i class="fa fa-external-link"/>
                    Ver FEL
                </a>
                <button t-else=""
                        type="button"
                        class="btn btn-sm btn-outline-secondary"
                        data-bs-toggle="modal"
                        t-att-data-bs-target="'#felNotCertifiedModal%s' % invoice.id">
                    <i class="fa fa-file-text-o"/>
                    No certificado
                </button>
                <div t-if="not fel_document_url"
                     class="modal fade"
                     t-att-id="'felNotCertifiedModal%s' % invoice.id"
                     tabindex="-1"
                     t-att-aria-labelledby="'felNotCertifiedModalLabel%s' % invoice.id"
                     aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered">
                        <div class="modal-content text-start">
                            <div class="modal-header">
                                <h5 class="modal-title"
                                    t-att-id="'felNotCertifiedModalLabel%s' % invoice.id">
                                    Documento FEL no disponible
                                </h5>
                                <button type="button"
                                        class="btn-close"
                                        data-bs-dismiss="modal"
                                        aria-label="Cerrar"/>
                            </div>
                            <div class="modal-body">
                                <p t-if="invoice.fel_documento_certificado" class="mb-0">
                                    La factura tiene un enlace FEL registrado, pero no es una
                                    URL HTTPS válida de Feel. Solicita una revisión interna.
                                </p>
                                <p t-else="" class="mb-0">
                                    La factura <strong t-out="invoice.name"/> todavía no ha
                                    sido certificada y no existe un documento FEL para descargar.
                                </p>
                            </div>
                            <div class="modal-footer">
                                <button type="button"
                                        class="btn btn-secondary"
                                        data-bs-dismiss="modal">
                                    Cerrar
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </td>
        </xpath>
    </template>

    <template id="portal_invoice_list_batch_payment"
              name="Portal invoice batch payment"
              inherit_id="account.portal_my_invoices"
              priority="45"
              active="False">
        <xpath expr="//t[@t-if='invoices'][@t-call='portal.portal_table']" position="before">
            <div t-if="request.params.get('batch_proof_submitted')"
                 class="alert alert-success alert-dismissible d-print-none"
                 role="status">
                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="alert"
                        aria-label="Cerrar"/>
                El comprobante fue recibido para las facturas seleccionadas. Contabilidad
                todavía debe revisar y registrar el pago.
            </div>
            <div t-if="request.params.get('batch_proof_error')"
                 class="alert alert-danger alert-dismissible d-print-none"
                 role="alert">
                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="alert"
                        aria-label="Cerrar"/>
                <t t-if="request.params.get('batch_proof_error') == 'selection'">
                    Selecciona al menos una factura válida.
                </t>
                <t t-elif="request.params.get('batch_proof_error') == 'orders'">
                    Todas las facturas deben proceder de una orden de venta confirmada.
                </t>
                <t t-elif="request.params.get('batch_proof_error') == 'currency'">
                    Las facturas seleccionadas deben usar la misma compañía y moneda.
                </t>
                <t t-elif="request.params.get('batch_proof_error') == 'missing'">
                    Adjunta un comprobante.
                </t>
                <t t-elif="request.params.get('batch_proof_error') == 'size'">
                    El archivo supera el límite de 10 MB.
                </t>
                <t t-elif="request.params.get('batch_proof_error') == 'type'">
                    Usa una imagen JPG, PNG, WEBP o un archivo PDF.
                </t>
                <t t-else="">Revisa el monto, la fecha y las facturas seleccionadas.</t>
            </div>
            <div t-if="invoices"
                 class="d-flex justify-content-end mb-3 d-print-none">
                <button id="portalBatchPaymentButton"
                        type="button"
                        class="btn btn-success"
                        disabled="disabled"
                        data-bs-toggle="modal"
                        data-bs-target="#portalBatchPaymentModal">
                    <i class="fa fa-credit-card me-1"/> Pagar seleccionadas
                </button>
            </div>
        </xpath>

        <xpath expr="//th[@name='invoice_number']" position="before">
            <th name="batch_payment_selection" class="d-print-none">
                <input id="portalBatchPaymentSelectAll"
                       type="checkbox"
                       class="form-check-input"
                       title="Seleccionar todas las facturas pagables"
                       aria-label="Seleccionar todas las facturas pagables"/>
            </th>
        </xpath>
        <xpath expr="//th[@name='invoice_number']" position="after">
            <th name="fel_series" class="d-none d-lg-table-cell">Serie FEL</th>
            <th name="fel_number" class="d-none d-lg-table-cell">Número FEL</th>
        </xpath>
        <xpath expr="//tbody/t/tr/td[a[@t-att-href='invoice.get_portal_url()']]"
               position="before">
            <td name="batch_payment_selection" class="d-print-none">
                <input t-if="invoice._is_portal_batch_payment_eligible()"
                       type="checkbox"
                       name="invoice_ids"
                       t-att-value="invoice.id"
                       t-att-data-amount="invoice.amount_residual"
                       form="portalBatchPaymentForm"
                       class="form-check-input o_portal_batch_invoice"
                       t-att-aria-label="'Seleccionar factura %s' % invoice.name"/>
                <span t-else="" class="text-muted">-</span>
            </td>
        </xpath>
        <xpath expr="//tbody/t/tr/td[a[@t-att-href='invoice.get_portal_url()']]"
               position="after">
            <td name="invoice_fel_series" class="d-none d-lg-table-cell">
                <span t-out="invoice._get_portal_fel_series() or '-'"/>
            </td>
            <td name="invoice_fel_number" class="d-none d-lg-table-cell">
                <span t-out="invoice._get_portal_fel_number() or '-'"/>
            </td>
        </xpath>

        <xpath expr="//t[@t-if='invoices'][@t-call='portal.portal_table']" position="after">
            <div class="modal fade"
                 id="portalBatchPaymentModal"
                 tabindex="-1"
                 aria-labelledby="portalBatchPaymentModalLabel"
                 aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <form id="portalBatchPaymentForm"
                              method="post"
                              enctype="multipart/form-data"
                              action="/my/invoices/payment-proof">
                            <input type="hidden"
                                   name="csrf_token"
                                   t-att-value="request.csrf_token()"/>
                            <div class="modal-header">
                                <h5 class="modal-title" id="portalBatchPaymentModalLabel">
                                    Reportar pago de facturas
                                </h5>
                                <button type="button"
                                        class="btn-close"
                                        data-bs-dismiss="modal"
                                        aria-label="Cerrar"/>
                            </div>
                            <div class="modal-body">
                                <p class="text-muted">
                                    El comprobante se asociará a todas las facturas
                                    seleccionadas. Esto no creará un pago contable.
                                </p>
                                <div class="mb-3">
                                    <label for="portal_batch_payment_amount" class="form-label">
                                        Monto reportado
                                    </label>
                                    <input id="portal_batch_payment_amount"
                                           name="amount"
                                           type="number"
                                           min="0.01"
                                           step="0.01"
                                           required="required"
                                           class="form-control"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_batch_payment_date" class="form-label">
                                        Fecha del pago
                                    </label>
                                    <input id="portal_batch_payment_date"
                                           name="payment_date"
                                           type="date"
                                           required="required"
                                           class="form-control"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_batch_payment_file" class="form-label">
                                        Comprobante
                                    </label>
                                    <input id="portal_batch_payment_file"
                                           name="payment_proof"
                                           type="file"
                                           accept="image/jpeg,image/png,image/webp,application/pdf"
                                           required="required"
                                           class="form-control"/>
                                    <div class="form-text">
                                        JPG, PNG, WEBP o PDF; máximo 10 MB.
                                    </div>
                                </div>
                                <div>
                                    <label for="portal_batch_payment_note" class="form-label">
                                        Notas
                                    </label>
                                    <textarea id="portal_batch_payment_note"
                                              name="note"
                                              maxlength="2000"
                                              rows="3"
                                              class="form-control"/>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button"
                                        class="btn btn-outline-secondary"
                                        data-bs-dismiss="modal">
                                    Cancelar
                                </button>
                                <button type="submit" class="btn btn-success">
                                    Enviar comprobante
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_invoice_list_fel_identifiers"
              name="Serie y número FEL en lista de facturas"
              inherit_id="account.portal_my_invoices"
              priority="46">
        <xpath expr="//th[@name='invoice_number']" position="after">
            <th name="fel_series" class="d-none d-lg-table-cell">Serie FEL</th>
            <th name="fel_number" class="d-none d-lg-table-cell">Número FEL</th>
        </xpath>
        <xpath expr="//tbody/t/tr/td[a[@t-att-href='invoice.get_portal_url()']]"
               position="after">
            <td name="invoice_fel_series" class="d-none d-lg-table-cell">
                <span t-out="invoice._get_portal_fel_series() or '-'"/>
            </td>
            <td name="invoice_fel_number" class="d-none d-lg-table-cell">
                <span t-out="invoice._get_portal_fel_number() or '-'"/>
            </td>
        </xpath>
    </template>

    <template id="portal_breadcrumbs_partner_self_service"
              name="Navegación del portal de autoservicio"
              inherit_id="portal.portal_breadcrumbs"
              priority="50">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'portal_inventory'" class="breadcrumb-item active">
                Inventario
            </li>
            <li t-if="page_name == 'portal_payments'" class="breadcrumb-item active">
                Pagos
            </li>
            <li t-if="page_name in ('portal_purchase_requests', 'portal_purchase_request', 'portal_purchase_request_form')"
                t-attf-class="breadcrumb-item #{'active' if page_name == 'portal_purchase_requests' else ''}">
                <a t-if="page_name != 'portal_purchase_requests'" href="/my/purchase-requests">Solicitudes</a>
                <t t-else="">Solicitudes</t>
            </li>
            <li t-if="page_name == 'portal_purchase_request'" class="breadcrumb-item active">
                <t t-out="purchase_request.name"/>
            </li>
            <li t-if="page_name == 'portal_purchase_request_form'" class="breadcrumb-item active">
                <t t-if="purchase_request">Editar</t>
                <t t-else="">Nueva</t>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_partner_self_service"
              name="Accesos del portal de autoservicio"
              inherit_id="portal.portal_my_home"
              customize_show="True"
              priority="50">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_client_category_enable" t-value="True"/>
        </xpath>
        <div id="portal_client_category" position="inside">
            <t t-if="request.env.user.partner_id.portal_user_type != 'project'"
               t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/stock/static/description/icon.png'"/>
                <t t-set="title">Inventario disponible</t>
                <t t-set="url" t-value="'/my/inventory'"/>
                <t t-set="text">Consulta existencias en tiempo real de tu almacén</t>
                <t t-set="placeholder_count" t-value="'portal_inventory_count'"/>
                <t t-set="config_card" t-value="True"/>
            </t>
            <t t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/sale/static/src/img/bag.svg'"/>
                <t t-set="title">Solicitudes de compra</t>
                <t t-set="url" t-value="'/my/purchase-requests'"/>
                <t t-set="text">Crea y revisa solicitudes de tu compañía</t>
                <t t-set="placeholder_count" t-value="'purchase_request_count'"/>
                <t t-set="config_card" t-value="True"/>
            </t>
            <t t-if="request.env.user.partner_id.portal_user_type != 'project'"
               t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/account/static/src/img/Bill.svg'"/>
                <t t-set="title">Pagos registrados</t>
                <t t-set="url" t-value="'/my/payments'"/>
                <t t-set="text">Consulta los pagos registrados para tu compañía</t>
                <t t-set="placeholder_count" t-value="'portal_payment_count'"/>
                <t t-set="config_card" t-value="True"/>
            </t>
        </div>
    </template>

    <template id="portal_configuration_required" name="Configuración requerida">
        <t t-call="portal.portal_layout">
            <div class="alert alert-warning mt-3" role="alert">
                <h4 class="alert-heading">Configuración pendiente</h4>
                <p t-if="not company_partner">
                    Tu usuario debe estar asociado a un contacto que pertenezca a una compañía.
                    Solicita al administrador de Odoo que revise tu contacto padre.
                </p>
                <p t-elif="not warehouse">
                    Tu compañía todavía no tiene un almacén activo asignado para el portal.
                    Solicita al administrador que lo configure en la ficha de la compañía cliente.
                </p>
                <a class="btn btn-outline-primary" href="/my/home">Volver al portal</a>
            </div>
        </t>
    </template>

    <template id="portal_my_inventory" name="Inventario de mi compañía">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Inventario disponible</t>
            </t>

            <div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-3">
                <div>
                    <h5 class="mb-1" t-out="warehouse.display_name"/>
                    <span class="text-muted">Existencia libre: inventario físico menos reservas vigentes.</span>
                </div>
                <form method="get" action="/my/inventory" class="d-flex" role="search">
                    <input type="search" name="search" t-att-value="search"
                           class="form-control" placeholder="Buscar producto, código o código de barras..."/>
                    <button type="submit" class="btn btn-outline-primary ms-2">Buscar</button>
                </form>
            </div>

            <div t-if="not inventory_items" class="alert alert-info" role="status">
                No hay productos disponibles para los criterios indicados.
            </div>
            <t t-if="inventory_items" t-call="portal.portal_table">
                <thead>
                    <tr>
                        <th>Código</th>
                        <th>Producto</th>
                        <th class="text-end">Disponible</th>
                        <th>Unidad</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="inventory_items" t-as="item">
                        <td><span t-out="item['product'].default_code or '-'"/></td>
                        <td><span t-out="item['product'].display_name"/></td>
                        <td class="text-end fw-semibold"><span t-out="item['available']"/></td>
                        <td><span t-out="item['product'].uom_id.name"/></td>
                    </tr>
                </tbody>
            </t>
            <div class="mt-3 text-end">
                <a class="btn btn-primary" href="/my/purchase-requests/new">
                    Crear solicitud
                </a>
            </div>
        </t>
    </template>

    <template id="portal_my_payments" name="Pagos de mi compañía">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Pagos registrados</t>
            </t>
            <div t-if="not payments" class="alert alert-info" role="status">
                No hay pagos procesados o pagados registrados para tu compañía.
            </div>
            <t t-if="payments" t-call="portal.portal_table">
                <thead>
                    <tr>
                        <th>Referencia</th>
                        <th>Fecha</th>
                        <th>Concepto</th>
                        <th class="text-end">Monto</th>
                        <th>Estado</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="payments" t-as="payment">
                        <td><span t-out="payment.name or '-'"/></td>
                        <td><span t-field="payment.date"/></td>
                        <td><span t-out="payment.payment_reference or payment.memo or '-'"/></td>
                        <td class="text-end">
                            <span t-field="payment.amount"
                                  t-options="{'widget': 'monetary', 'display_currency': payment.currency_id}"/>
                        </td>
                        <td>
                            <span t-if="payment.state == 'paid'" class="badge rounded-pill text-bg-success">Pagado</span>
                            <span t-else="" class="badge rounded-pill text-bg-info">En proceso</span>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <template id="portal_my_purchase_requests" name="Mis solicitudes de compra">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Solicitudes de compra</t>
            </t>
            <div class="d-flex justify-content-end mb-3">
                <a t-if="warehouse" class="btn btn-primary" href="/my/purchase-requests/new">
                    Nueva solicitud
                </a>
            </div>
            <div t-if="not purchase_requests" class="alert alert-info" role="status">
                Todavía no hay solicitudes para tu compañía.
            </div>
            <t t-if="purchase_requests" t-call="portal.portal_table">
                <thead>
                    <tr>
                        <th>Correlativo</th>
                        <th>Fecha</th>
                        <th>Proyecto</th>
                        <th>Solicitado por</th>
                        <th class="text-end">Cantidad</th>
                        <th>Orden de venta</th>
                        <th>Estado</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="purchase_requests" t-as="purchase_request">
                        <td>
                            <a t-att-href="purchase_request.get_portal_url()">
                                <span t-out="purchase_request.name"/>
                            </a>
                        </td>
                        <td><span t-field="purchase_request.create_date" t-options="{'widget': 'date'}"/></td>
                        <td><span t-out="purchase_request.customer_reference or '-'"/></td>
                        <td><span t-out="purchase_request.requested_by_id.name"/></td>
                        <td class="text-end"><span t-out="purchase_request.total_requested_qty"/></td>
                        <td>
                            <a t-if="purchase_request.sale_order_id and is_office_user"
                               t-att-href="purchase_request.sale_order_id.get_portal_url()">
                                <span t-out="purchase_request.sale_order_id.name"/>
                            </a>
                            <span t-elif="purchase_request.sale_order_id"
                                  t-out="purchase_request.sale_order_id.name"/>
                            <span t-else="">-</span>
                        </td>
                        <td>
                            <span t-if="purchase_request.state == 'confirmed'"
                                  class="badge rounded-pill text-bg-success">Confirmada</span>
                            <span t-else="" class="badge rounded-pill text-bg-info">Borrador</span>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <template id="portal_purchase_request_detail" name="Detalle de solicitud de compra">
        <t t-call="portal.portal_layout">
            <div t-if="created" class="alert alert-success" role="status">
                La solicitud se guardó como borrador.
            </div>
            <div t-if="confirmed" class="alert alert-success" role="status">
                La solicitud fue confirmada y se creó la orden de venta.
            </div>
            <div t-if="confirmation_error" class="alert alert-danger" role="alert">
                <span t-out="confirmation_error"/>
            </div>
            <div t-if="authorization_required" class="alert alert-warning" role="alert">
                Tu usuario de proyecto puede crear y consultar solicitudes, pero solo un
                usuario de oficina puede confirmarlas.
            </div>

            <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
                <div>
                    <h2 class="mb-1" t-out="purchase_request.name"/>
                    <span class="text-muted" t-out="purchase_request.warehouse_id.display_name"/>
                </div>
                <div class="d-flex gap-2 d-print-none">
                    <a t-attf-href="/my/purchase-requests/{{ purchase_request.id }}/report"
                       class="btn btn-outline-secondary">
                        <i class="fa fa-print me-1"/> Imprimir
                    </a>
                    <a t-if="purchase_request.state == 'draft'
                             and (is_office_user
                                  or purchase_request.requested_by_id == contact_partner)"
                       t-attf-href="/my/purchase-requests/{{ purchase_request.id }}/edit"
                       class="btn btn-outline-primary">Editar</a>
                    <button t-if="purchase_request.state == 'draft' and is_office_user"
                            type="button"
                            class="btn btn-primary"
                            data-bs-toggle="modal"
                            data-bs-target="#portalConfirmationKeyModal">
                        Confirmar
                    </button>
                    <a t-if="purchase_request.sale_order_id and is_office_user"
                       t-att-href="purchase_request.sale_order_id.get_portal_url()"
                       class="btn btn-success">Ver orden de venta</a>
                </div>
            </div>

            <div class="row mb-4">
                <div class="col-md-6">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">Compañía</dt>
                        <dd class="col-sm-7" t-out="purchase_request.partner_id.display_name"/>
                        <dt class="col-sm-5">Solicitado por</dt>
                        <dd class="col-sm-7" t-out="purchase_request.requested_by_id.name"/>
                        <dt class="col-sm-5">Proyecto</dt>
                        <dd class="col-sm-7" t-out="purchase_request.customer_reference or '-'"/>
                    </dl>
                </div>
                <div class="col-md-6">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">Estado</dt>
                        <dd class="col-sm-7">
                            <span t-if="purchase_request.state == 'confirmed'"
                                  class="badge rounded-pill text-bg-success">Confirmada</span>
                            <span t-else="" class="badge rounded-pill text-bg-info">Borrador</span>
                        </dd>
                        <dt class="col-sm-5">Creada</dt>
                        <dd class="col-sm-7"><span t-field="purchase_request.create_date"/></dd>
                        <dt t-if="purchase_request.confirmed_at" class="col-sm-5">Confirmada</dt>
                        <dd t-if="purchase_request.confirmed_at" class="col-sm-7">
                            <span t-field="purchase_request.confirmed_at"/>
                        </dd>
                        <dt t-if="purchase_request.authorized_by_id" class="col-sm-5">
                            Autorizado por
                        </dt>
                        <dd t-if="purchase_request.authorized_by_id" class="col-sm-7"
                            t-out="purchase_request.authorized_by_id.name"/>
                    </dl>
                </div>
            </div>

            <t t-call="portal.portal_table">
                <thead>
                    <tr>
                        <th>Código</th>
                        <th>Producto</th>
                        <th class="text-end">Cantidad</th>
                        <th class="text-end">Pendiente</th>
                        <th>Unidad</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="purchase_request.line_ids" t-as="line">
                        <td><span t-out="line.product_id.default_code or '-'"/></td>
                        <td><span t-out="line.product_id.display_name"/></td>
                        <td class="text-end"><span t-out="line.product_uom_qty"/></td>
                        <td class="text-end"><span t-out="line.pending_qty"/></td>
                        <td><span t-out="line.product_uom_id.name"/></td>
                    </tr>
                </tbody>
            </t>
            <div t-if="purchase_request.note" class="card mt-4">
                <div class="card-header">Notas</div>
                <div class="card-body"><span t-out="purchase_request.note"/></div>
            </div>

            <div t-if="purchase_request.state == 'draft' and is_office_user"
                 class="modal fade"
                 id="portalConfirmationKeyModal"
                 tabindex="-1"
                 aria-labelledby="portalConfirmationKeyModalLabel"
                 aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <form method="post"
                              t-attf-action="/my/purchase-requests/{{ purchase_request.id }}/confirm">
                            <input type="hidden"
                                   name="csrf_token"
                                   t-att-value="request.csrf_token()"/>
                            <div class="modal-header">
                                <h5 class="modal-title" id="portalConfirmationKeyModalLabel">
                                    Autorizar compra
                                </h5>
                                <button type="button"
                                        class="btn-close"
                                        data-bs-dismiss="modal"
                                        aria-label="Cerrar"/>
                            </div>
                            <div class="modal-body">
                                <p class="text-muted">
                                    Ingresa tu clave personal para confirmar la solicitud
                                    <strong t-out="purchase_request.name"/>.
                                </p>
                                <label for="portal_confirmation_key" class="form-label">
                                    Clave de confirmación
                                </label>
                                <input id="portal_confirmation_key"
                                       name="confirmation_key"
                                       type="password"
                                       minlength="6"
                                       maxlength="128"
                                       autocomplete="current-password"
                                       required="required"
                                       class="form-control"/>
                                <div class="mt-3">
                                    <label for="portal_confirmation_note" class="form-label">
                                        Notas
                                    </label>
                                    <textarea id="portal_confirmation_note"
                                              name="confirmation_note"
                                              maxlength="2000"
                                              rows="3"
                                              class="form-control"
                                              placeholder="Agrega una nota para la orden de venta..."/>
                                    <div class="form-text">
                                        La nota se guardará únicamente si la clave es correcta
                                        y la solicitud logra confirmarse.
                                    </div>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button"
                                        class="btn btn-outline-secondary"
                                        data-bs-dismiss="modal">
                                    Cancelar
                                </button>
                                <button type="submit" class="btn btn-primary">
                                    Confirmar compra
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_purchase_request_form" name="Formulario de solicitud de compra">
        <t t-call="portal.portal_layout">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <h2 class="mb-1">
                        <t t-if="purchase_request">Editar <span t-out="purchase_request.name"/></t>
                        <t t-else="">Nueva solicitud</t>
                    </h2>
                    <span class="text-muted" t-out="warehouse.display_name"/>
                </div>
                <a class="btn btn-outline-secondary" href="/my/purchase-requests">Cancelar</a>
            </div>

            <div t-if="errors" class="alert alert-danger" role="alert">
                <ul class="mb-0">
                    <li t-foreach="errors" t-as="error"><span t-out="error"/></li>
                </ul>
            </div>
            <div t-if="not inventory_items" class="alert alert-warning" role="alert">
                No hay inventario libre disponible para crear una solicitud.
            </div>

            <form method="post"
                  t-att-action="('/my/purchase-requests/%s/edit' % purchase_request.id) if purchase_request else '/my/purchase-requests/new'">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="row g-3 mb-4">
                    <div class="col-md-6">
                        <label for="customer_reference" class="form-label">Proyecto</label>
                        <input id="customer_reference" name="customer_reference" type="text"
                               maxlength="128" class="form-control" t-att-value="form_reference"/>
                    </div>
                    <div class="col-12">
                        <label for="note" class="form-label">Notas</label>
                        <textarea id="note" name="note" maxlength="2000" rows="3"
                                  class="form-control" t-out="form_note"/>
                    </div>
                </div>

                <div t-if="inventory_items" class="table-responsive">
                    <table class="table table-hover align-middle o_portal_request_products">
                        <thead>
                            <tr>
                                <th>Código</th>
                                <th>Producto</th>
                                <th class="text-end">Disponible</th>
                                <th style="min-width: 10rem;">Cantidad solicitada</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="inventory_items" t-as="item">
                                <t t-set="product" t-value="item['product']"/>
                                <td><span t-out="product.default_code or '-'"/></td>
                                <td>
                                    <span t-out="product.display_name"/>
                                    <small class="d-block text-muted" t-out="product.uom_id.name"/>
                                </td>
                                <td class="text-end fw-semibold"><span t-out="item['available']"/></td>
                                <td>
                                    <input type="number" min="0" step="any"
                                           t-att-max="item['available']"
                                           t-att-name="'qty_%s' % product.id"
                                           t-att-value="current_quantities.get(product.id, '')"
                                           class="form-control"/>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                <div class="d-flex justify-content-end gap-2 mt-3">
                    <a class="btn btn-outline-secondary" href="/my/purchase-requests">Cancelar</a>
                    <button type="submit" class="btn btn-primary" t-att-disabled="not inventory_items">
                        Guardar borrador
                    </button>
                </div>
            </form>
        </t>
    </template>

    <template id="portal_customer_brand"
              name="Identidad de la compañía cliente en el portal"
              inherit_id="portal.portal_layout"
              priority="60">
        <xpath expr="//div[@id='wrap']/div[hasclass('container')]/t[1]" position="before">
            <t t-if="not request.env.user._is_public() and request.env.user.share">
                <t t-set="portal_customer_company"
                   t-value="request.env.user.partner_id.commercial_partner_id.sudo()"/>
                <div t-if="portal_customer_company != request.env.user.partner_id"
                     class="o_portal_customer_brand d-flex align-items-center gap-3 py-3 mb-2 border-bottom">
                    <img t-if="portal_customer_company.image_128"
                         t-att-src="image_data_uri(portal_customer_company.image_128)"
                         t-att-alt="portal_customer_company.name"
                         class="rounded object-fit-contain"
                         style="width: 64px; height: 64px;"/>
                    <div>
                        <small class="text-muted d-block">Portal de la compañía</small>
                        <strong class="fs-5" t-out="portal_customer_company.name"/>
                    </div>
                </div>
            </t>
        </xpath>
    </template>

    <template id="portal_my_home_project_visibility"
              name="Accesos limitados para usuarios de proyecto"
              inherit_id="portal.portal_my_home"
              priority="100">
        <xpath expr="//t[@t-call='portal.portal_docs_entry'][t[@t-set='url' and contains(@t-value, '/my/orders')]]"
               position="attributes">
            <attribute name="t-if">request.env.user.partner_id.portal_user_type != 'project'</attribute>
        </xpath>
        <xpath expr="//t[@t-call='portal.portal_docs_entry'][t[@t-set='url' and contains(@t-value, '/my/quotes')]]"
               position="attributes">
            <attribute name="t-if">request.env.user.partner_id.portal_user_type != 'project'</attribute>
        </xpath>
        <xpath expr="//t[@t-call='portal.portal_docs_entry'][t[@t-set='url' and contains(@t-value, '/my/invoices')]]"
               position="attributes">
            <attribute name="t-if">request.env.user.partner_id.portal_user_type != 'project'</attribute>
        </xpath>
        <xpath expr="//t[@t-call='portal.portal_docs_entry'][t[@t-set='url' and contains(@t-value, '/my/security')]]"
               position="attributes">
            <attribute name="t-if">request.env.user.partner_id.portal_user_type != 'project'</attribute>
        </xpath>
    </template>

    <template id="portal_sale_order_list_self_service"
              name="Selección y trazabilidad de órdenes de venta"
              inherit_id="sale.portal_my_orders"
              priority="60">
        <xpath expr="//t[@t-if='orders'][@t-call='portal.portal_table']" position="before">
            <div t-if="request.params.get('batch_proof_submitted')"
                 class="alert alert-success alert-dismissible d-print-none"
                 role="status">
                <button type="button" class="btn-close" data-bs-dismiss="alert"
                        aria-label="Cerrar"/>
                El comprobante fue recibido para las órdenes seleccionadas. Contabilidad
                todavía debe revisarlo y registrar el pago.
            </div>
            <div t-if="request.params.get('batch_proof_error') or request.params.get('statement_error')"
                 class="alert alert-danger alert-dismissible d-print-none"
                 role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert"
                        aria-label="Cerrar"/>
                <t t-set="portal_order_error"
                   t-value="request.params.get('batch_proof_error') or request.params.get('statement_error')"/>
                <t t-if="portal_order_error == 'selection'">
                    Selecciona al menos una orden de venta válida.
                </t>
                <t t-elif="portal_order_error == 'currency'">
                    Las órdenes seleccionadas deben usar la misma compañía y moneda.
                </t>
                <t t-elif="portal_order_error == 'missing'">Adjunta un comprobante.</t>
                <t t-elif="portal_order_error == 'size'">
                    El archivo supera el límite de 10 MB.
                </t>
                <t t-elif="portal_order_error == 'type'">
                    Usa una imagen JPG, PNG, WEBP o un archivo PDF.
                </t>
                <t t-else="">Revisa el monto, la fecha y las órdenes seleccionadas.</t>
            </div>
            <div t-if="orders and request.env.user.partner_id.portal_user_type != 'project'"
                 class="d-flex flex-wrap justify-content-end gap-2 mb-3 d-print-none">
                <form id="portalOrderStatementForm"
                      method="post"
                      action="/my/orders/account-statement"
                      target="_blank">
                    <input type="hidden" name="csrf_token"
                           t-att-value="request.csrf_token()"/>
                    <button id="portalOrderStatementButton"
                            type="submit"
                            class="btn btn-outline-primary"
                            disabled="disabled">
                        <i class="fa fa-file-pdf-o me-1"/> Imprimir estado de cuenta
                    </button>
                </form>
                <button id="portalOrderBatchPaymentButton"
                        type="button"
                        class="btn btn-success"
                        disabled="disabled"
                        data-bs-toggle="modal"
                        data-bs-target="#portalOrderBatchPaymentModal">
                    <i class="fa fa-credit-card me-1"/> Pagar seleccionadas
                </button>
            </div>
        </xpath>

        <xpath expr="//thead/tr/th[1]" position="before">
            <th t-if="request.env.user.partner_id.portal_user_type != 'project'"
                name="batch_order_selection"
                class="d-print-none">
                <input id="portalOrderSelectAll"
                       type="checkbox"
                       class="form-check-input"
                       title="Seleccionar todas las órdenes"
                       aria-label="Seleccionar todas las órdenes"/>
            </th>
        </xpath>
        <xpath expr="//thead/tr/th[1]" position="after">
            <th class="d-none d-lg-table-cell">Proyecto</th>
            <th class="d-none d-lg-table-cell">Solicitado por</th>
            <th class="d-none d-lg-table-cell">Autorizado por</th>
        </xpath>
        <xpath expr="//t[@t-foreach='orders']/tr/td[a[@t-att-href='order.get_portal_url()']]"
               position="before">
            <td t-if="request.env.user.partner_id.portal_user_type != 'project'"
                name="batch_order_selection"
                class="d-print-none">
                <input type="checkbox"
                       name="order_ids"
                       t-att-value="order.id"
                       t-att-data-amount="order.amount_total"
                       form="portalOrderStatementForm"
                       class="form-check-input o_portal_batch_order"
                       t-att-aria-label="'Seleccionar orden %s' % order.name"/>
            </td>
        </xpath>
        <xpath expr="//t[@t-foreach='orders']/tr/td[a[@t-att-href='order.get_portal_url()']]"
               position="after">
            <td class="d-none d-lg-table-cell">
                <span t-out="order.portal_project_name or '-'"/>
            </td>
            <td class="d-none d-lg-table-cell">
                <span t-out="order.portal_requested_by_id.name or '-'"/>
            </td>
            <td class="d-none d-lg-table-cell">
                <span t-out="order.portal_authorized_by_id.name or '-'"/>
            </td>
        </xpath>

        <xpath expr="//t[@t-if='orders'][@t-call='portal.portal_table']" position="after">
            <div t-if="request.env.user.partner_id.portal_user_type != 'project'"
                 class="modal fade"
                 id="portalOrderBatchPaymentModal"
                 tabindex="-1"
                 aria-labelledby="portalOrderBatchPaymentModalLabel"
                 aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <form id="portalOrderBatchPaymentForm"
                              method="post"
                              enctype="multipart/form-data"
                              action="/my/orders/payment-proof">
                            <input type="hidden" name="csrf_token"
                                   t-att-value="request.csrf_token()"/>
                            <div class="modal-header">
                                <h5 class="modal-title" id="portalOrderBatchPaymentModalLabel">
                                    Reportar pago de órdenes
                                </h5>
                                <button type="button" class="btn-close"
                                        data-bs-dismiss="modal" aria-label="Cerrar"/>
                            </div>
                            <div class="modal-body">
                                <p class="text-muted">
                                    El comprobante se asociará a todas las órdenes seleccionadas.
                                    Esto no creará un pago contable.
                                </p>
                                <div class="mb-3">
                                    <label for="portal_order_batch_payment_amount"
                                           class="form-label">Monto reportado</label>
                                    <input id="portal_order_batch_payment_amount"
                                           name="amount" type="number" min="0.01" step="0.01"
                                           required="required" class="form-control"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_order_batch_payment_date"
                                           class="form-label">Fecha del pago</label>
                                    <input id="portal_order_batch_payment_date"
                                           name="payment_date" type="date" required="required"
                                           class="form-control"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_order_batch_payment_file"
                                           class="form-label">Comprobante</label>
                                    <input id="portal_order_batch_payment_file"
                                           name="payment_proof" type="file"
                                           accept="image/jpeg,image/png,image/webp,application/pdf"
                                           required="required" class="form-control"/>
                                    <div class="form-text">JPG, PNG, WEBP o PDF; máximo 10 MB.</div>
                                </div>
                                <div>
                                    <label for="portal_order_batch_payment_note"
                                           class="form-label">Notas</label>
                                    <textarea id="portal_order_batch_payment_note"
                                              name="note" maxlength="2000" rows="3"
                                              class="form-control"/>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-outline-secondary"
                                        data-bs-dismiss="modal">Cancelar</button>
                                <button type="submit" class="btn btn-success">
                                    Enviar comprobante
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_sale_order_traceability"
              name="Trazabilidad de solicitud en orden de venta"
              inherit_id="sale.sale_order_portal_content"
              priority="60">
        <xpath expr="//tbody[@id='sale_info_table']" position="inside">
            <tr t-if="sale_order.portal_order_request_id">
                <th class="ps-0 pb-0">Proyecto:</th>
                <td class="w-100 pb-0 text-wrap">
                    <span t-out="sale_order.portal_project_name or '-'"/>
                </td>
            </tr>
            <tr t-if="sale_order.portal_order_request_id">
                <th class="ps-0 pb-0">Solicitado por:</th>
                <td class="w-100 pb-0 text-wrap">
                    <span t-out="sale_order.portal_requested_by_id.name or '-'"/>
                </td>
            </tr>
            <tr t-if="sale_order.portal_order_request_id">
                <th class="ps-0 pb-0">Autorizado por:</th>
                <td class="w-100 pb-0 text-wrap">
                    <span t-out="sale_order.portal_authorized_by_id.name or '-'"/>
                </td>
            </tr>
        </xpath>
    </template>

    <template id="sale_order_portal_payment_proof"
              name="Comprobante de pago en orden de venta"
              inherit_id="sale.sale_order_portal_template"
              priority="50">
        <xpath expr="//div[@id='sale_order_sidebar_button']" position="inside">
            <a t-if="sale_order.state == 'sale'
                     and not request.env.user._is_public()
                     and request.env.user.partner_id.portal_user_type != 'project'
                     and sale_order.partner_id.commercial_partner_id == request.env.user.partner_id.commercial_partner_id"
               role="button" class="btn btn-success"
               data-bs-toggle="modal" data-bs-target="#portalPaymentProofModal" href="#">
                <i class="fa fa-credit-card me-1"/> Pagar
            </a>
        </xpath>
        <xpath expr="//div[hasclass('o_portal_sale_sidebar')]" position="after">
            <div t-if="request.params.get('proof_submitted')"
                 class="alert alert-success alert-dismissible d-print-none" role="status">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"/>
                El comprobante fue recibido. El pago aún debe ser registrado por contabilidad.
            </div>
            <div t-if="request.params.get('proof_error')"
                 class="alert alert-danger alert-dismissible d-print-none" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"/>
                <t t-if="request.params.get('proof_error') == 'missing'">Adjunta un comprobante.</t>
                <t t-elif="request.params.get('proof_error') == 'size'">El archivo supera el límite de 10 MB.</t>
                <t t-elif="request.params.get('proof_error') == 'type'">Usa una imagen JPG, PNG, WEBP o un archivo PDF.</t>
                <t t-else="">Revisa el monto y la fecha del pago.</t>
            </div>

            <div t-if="sale_order.state == 'sale'
                       and not request.env.user._is_public()
                       and request.env.user.partner_id.portal_user_type != 'project'
                       and sale_order.partner_id.commercial_partner_id == request.env.user.partner_id.commercial_partner_id"
                 class="modal fade" id="portalPaymentProofModal" tabindex="-1"
                 aria-labelledby="portalPaymentProofModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <form method="post" enctype="multipart/form-data"
                              t-att-action="'/my/orders/%s/payment-proof' % sale_order.id">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="modal-header">
                                <h5 class="modal-title" id="portalPaymentProofModalLabel">Reportar pago</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"/>
                            </div>
                            <div class="modal-body">
                                <p class="text-muted">
                                    Adjunta el comprobante de la orden <strong t-out="sale_order.name"/>.
                                    Esto notificará a contabilidad, pero no creará un pago contable.
                                </p>
                                <div class="mb-3">
                                    <label for="portal_payment_amount" class="form-label">Monto reportado</label>
                                    <input id="portal_payment_amount" name="amount" type="number"
                                           min="0.01" step="0.01" required="required"
                                           class="form-control" t-att-value="sale_order.amount_total"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_payment_date" class="form-label">Fecha del pago</label>
                                    <input id="portal_payment_date" name="payment_date" type="date"
                                           required="required" class="form-control"/>
                                </div>
                                <div class="mb-3">
                                    <label for="portal_payment_file" class="form-label">Comprobante</label>
                                    <input id="portal_payment_file" name="payment_proof" type="file"
                                           accept="image/jpeg,image/png,image/webp,application/pdf"
                                           required="required" class="form-control"/>
                                    <div class="form-text">JPG, PNG, WEBP o PDF; máximo 10 MB.</div>
                                </div>
                                <div>
                                    <label for="portal_payment_note" class="form-label">Notas</label>
                                    <textarea id="portal_payment_note" name="note" maxlength="2000"
                                              rows="3" class="form-control"/>
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                                <button type="submit" class="btn btn-success">Enviar comprobante</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
